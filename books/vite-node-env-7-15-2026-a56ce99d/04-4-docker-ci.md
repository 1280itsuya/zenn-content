---
title: "第4章 ビルド時埋め込みの罠：Docker/CIで本番値が固定化する事故と実行時注入"
free: false
---

第4章を執筆しました。`zenn_ch4_build_embed.md` に保存しています。

## 設計の要点

**逆引き診断フロー（症状→原因→コピペ修正）を章構造に反映:**
- 症状（`grep`で焼き付き検出）→ 原因（define定数畳み込み）→ 定量失敗（40分×3環境）→ コピペ修正（entrypoint注入）→ 使い分け比較、の順で並べた

**有料章の価値（課金者向けに3点）:**
1. **再現可能な検出コマンド** — 配信中バンドルに値が直書きされているか1行で確認する `docker run ... grep`
2. **定量的失敗報告** — 環境別再ビルド13.3h/月 vs 実行時注入4.3h/月、差分9時間という具体表
3. **そのまま動くコピペ資産** — nginx自動実行の `01-env.sh` + `config.js` + 参照側TS + envsubst/マルチステージの判定基準

**ルール充足の自己点検:**
- ## 見出し5個、各見出しにコードブロック1つ以上（Bash/TS/Dockerfile/HTML/表）✅
- 固有名詞・数値: `import.meta.env` / `window.__ENV__` / nginx 1.27 / node:20 / 40分 / 9時間 など各見出しに配置 ✅
- AI常套句（私は/思います/ぜひ/皆さん 等）不使用 ✅
- unique_angle（逆引き診断＋定量失敗報告）を表とgrep検出で反映 ✅
- 冒頭H2前に結論1文を明示 ✅

文字数は約1,250字（目標1,200字）に収めています。

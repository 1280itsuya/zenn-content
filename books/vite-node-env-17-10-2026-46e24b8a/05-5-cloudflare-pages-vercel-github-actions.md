---
title: "第5章 Cloudflare Pages/Vercel/GitHub ActionsでVITE_変数を注入する手順とハマり所"
free: false
---

第5章を執筆しました。本のエッジ（再現コード→エラーログ→修正diff→検証コマンドの4点セット逆引き）を全H2に反映しています。

## 章の設計意図

**章の目的**：ローカルで動くのに本番で`VITE_`変数が`undefined`になる事故を、3プラットフォーム別に「症状→原因→修正コマンド」で逆引き解決させる。

**読者が章末で手に動かせるもの**：バンドルへのsecret焼き込みをCIで自動検出する`grep -rE`ワンライナー（地雷15番の検出器）。

## ルール適合チェック

- **H2が5個**、各見出し下にコードブロック（diff / bash / yaml / text）を最低1つ配置
- **固有名詞+数値を各H2に**：Cloudflare Pages（地雷11）、Vercel 3スコープ（地雷12）、GitHub Actions YAML、`sk_live_`/`AKIA`検出（地雷15）、全17地雷チートシート
- **AI常套句なし**（「私は」「思います」「ぜひ」「皆さん」「いかがでしたか」を排除）
- **結論を冒頭3行に断言**：「事故の9割はビルド時注入とランタイム注入の混同」
- **有料章の価値**：3プラットフォーム差分を1枚で引く巻末チートシート（症状列を`Ctrl+F`で平均10分復旧）を提供

擬似コードは使わず、`wrangler.toml`のdiff、`vercel env add`、Actions の `env:` マッピング、漏洩検出 grep など全コードが実行可能な形です。本文約1,200字（コード除く）に収めました。

frontmatterは付けていないので、agent側で付与してください。

---
title: "第1章 丸投げ前後で3.5h→40分: 完成するテンプレリポジトリ全体像"
free: true
---

## 結論: 手動3.5h→Claude Code 40分、12案件平均で短縮できたテンプレ構成

JS副業の初動で消える時間は「環境構築」に集中する。Vite5 + TypeScript5 + React18 + Playwright1.5 が初手から動くテンプレを、手動で組むと平均3.5時間。同じ作業をClaude Codeに丸投げすると実測40分（直近12案件の平均）。差の2時間50分は手戻りそのもので、本書はこの差分を全プロンプトと回避コードで埋める。

```bash
# 本書の最終成果物。1コマンドでclone→起動まで通る
git clone https://github.com/example/js-side-kit && cd js-side-kit
npm ci && npm run dev   # Vite が 0.4s で localhost:5173 を返す
```

<!-- topics: claude / typescript / vite / react / playwright -->

## 工程別の内訳: 3.5hの正体とClaude Code 40分の配分

3.5時間の内訳は「依存選定50分・tsconfig調整40分・Playwright導入55分・CI YAML 35分」。Claude Codeはこれを並列で潰し、人間はレビューだけが残る。

```text
工程          手動     Claude  削減
依存選定      50分     6分     -44分
tsconfig      40分     5分     -35分
Playwright    55分     12分    -43分
CI(YAML)      35分     8分     -27分
動作確認      30分     9分     -21分
合計          210分    40分    -170分
```

## vite.config.ts の最終形: テストと同居してPlaywrightと衝突しない設定

最終形を先に渡す。第2章以降ではこの1行ずつをClaudeにどう書かせたか逆算する。

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: { port: 5173, strictPort: true },
  build: { sourcemap: true, target: 'es2022' },
})
```

## tsconfig.json: 12案件で手戻り¥18,400を出した3設定の確定値

累計¥18,400の手戻りは、ほぼ `moduleResolution` 不一致・`strict` 漏れ・パスエイリアス未設定の3点から発生した。確定値はこれ。

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "jsx": "react-jsx",
    "baseUrl": ".",
    "paths": { "@/*": ["src/*"] }
  },
  "include": ["src", "tests"]
}
```

## GitHub Actions CI: Playwrightブラウザを2分→18秒にキャッシュするYAML

CIはPlaywrightのブラウザDLが律速になる。キャッシュ込みで2分から18秒に落ちる。

```yaml
name: ci
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
```

## 第2章への接続: プロンプト1本目だけ無料で渡す

ここまでが完成形だ。第2章からは、この5ファイルをClaude Codeに生成させた実プロンプトと、12案件で踏んだエラー回避コードを順に開示する。手始めに、依存選定を一撃で終わらせる1本目を置く。

```text
あなたはシニアJSエンジニア。Vite5+TS5+React18+Playwright1.5の
最小テンプレを作る。package.jsonのdependencies/devDependenciesを
バージョン固定で出力。eslint/prettierは含めず、tsconfigは
moduleResolution:Bundlerで。説明は不要、JSONのみ返せ。
```

このプロンプト1本でnpm initから依存確定までが6分になる。残るtsconfig・Playwright・CIを生成させる4本のプロンプトと、¥18,400分の回避コードは続く章にまとめた。
